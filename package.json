{
  "name": "cargame",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n server,vite \"bun --hot src/server.ts\" \"vite\" ",
    "server": "bun --hot src/server.ts",
    "vite": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/bun": "^1.1.10",
    "concurrently": "^9.0.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "@techstark/opencv-js": "^4.10.0-release.1"
  }
}
