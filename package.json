{
  "name": "cargame",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n server,vite \"bun --watch src/server.ts\" \"vite\" ",
    "server": "bun --watch src/server.ts",
    "vite": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/bun": "^1.1.10",
    "concurrently": "^9.0.1",
    "gh-pages": "^6.1.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "@techstark/opencv-js": "^4.10.0-release.1",
    "js-logger": "^1.6.1"
  }
}
